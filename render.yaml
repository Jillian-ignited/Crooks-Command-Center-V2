services:
  - type: web
    name: crooks-command-center-v2
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main: app --host 0.0.0.0 --port $PORT
...
This file is clean, properly formatted,
    plan: free
